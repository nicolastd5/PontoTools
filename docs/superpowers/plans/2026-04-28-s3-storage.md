# S3 Storage Migration — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ativar o driver S3 já existente no backend, instalar a dependência `aws-sdk`, e migrar as fotos locais (~19MB) para o bucket `pontotools-fotos` na AWS.

**Architecture:** O backend já possui a abstração `backend/config/storage.js` com `localDriver` e `s3Driver`. Basta instalar o `aws-sdk`, corrigir o `async` desnecessário em `s3Driver.getUrl`, adicionar as variáveis de ambiente no servidor e rodar o script de migração. Nenhuma migration de banco é necessária — `photo_path` não muda.

**Tech Stack:** Node.js, `aws-sdk` v2, PostgreSQL (`pg`), PM2, SSH (chave `/c/Users/apoio/Downloads/nicolas.pem`)

---

## Mapa de arquivos

| Arquivo | Ação |
|---|---|
| `backend/package.json` | Modificar — adicionar `aws-sdk` |
| `backend/config/storage.js` | Modificar — remover `async` de `s3Driver.getUrl` |
| `backend/scripts/migrate-to-s3.js` | Criar — script de migração das fotos |

---

## Task 1: Instalar aws-sdk e corrigir storage.js

**Files:**
- Modify: `backend/package.json`
- Modify: `backend/config/storage.js:82`

- [ ] **Step 1: Instalar aws-sdk localmente**

```bash
cd backend
npm install aws-sdk
```

Esperado: `aws-sdk` adicionado ao `package.json` e `package-lock.json`.

- [ ] **Step 2: Verificar que aws-sdk foi adicionado ao package.json**

Abrir `backend/package.json` e confirmar que existe a linha:
```json
"aws-sdk": "^2.x.x"
```

- [ ] **Step 3: Corrigir s3Driver.getUrl em backend/config/storage.js**

Localizar a linha 82 (método `getUrl` do `s3Driver`). Alterar de:

```js
  async getUrl(key) {
    const AWS = require('aws-sdk');
    const s3  = new AWS.S3();
    return s3.getSignedUrlPromise('getObject', {
      Bucket:  process.env.AWS_BUCKET,
      Key:     key,
      Expires: 3600,
    });
  },
```

Para:

```js
  getUrl(key) {
    const AWS = require('aws-sdk');
    const s3  = new AWS.S3();
    return s3.getSignedUrlPromise('getObject', {
      Bucket:  process.env.AWS_BUCKET,
      Key:     key,
      Expires: 3600,
    });
  },
```

(Apenas remove o `async` — o retorno continua sendo uma Promise, o que é correto para `getSignedUrlPromise`.)

- [ ] **Step 4: Verificar que nenhum controller chama storage.getUrl**

```bash
grep -r "storage\.getUrl\|getUrl" backend/controllers/ backend/services/
```

Esperado: nenhum resultado (confirma que a mudança não afeta nenhum caller).

- [ ] **Step 5: Commit**

```bash
git add backend/package.json backend/package-lock.json backend/config/storage.js
git commit -m "feat(storage): instalar aws-sdk e corrigir getUrl no s3Driver"
```

---

## Task 2: Criar script de migração das fotos para S3

**Files:**
- Create: `backend/scripts/migrate-to-s3.js`

- [ ] **Step 1: Criar o diretório scripts se não existir**

```bash
mkdir -p backend/scripts
```

- [ ] **Step 2: Criar o arquivo backend/scripts/migrate-to-s3.js**

```js
// Script de migração única: copia fotos do disco local para o S3.
// Uso: node scripts/migrate-to-s3.js
// Requer: .env com DATABASE_URL, AWS_BUCKET, AWS_REGION, AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, PHOTOS_BASE_PATH

require('dotenv').config({ path: require('path').resolve(__dirname, '../.env') });

const fs   = require('fs');
const path = require('path');
const { Client } = require('pg');
const AWS  = require('aws-sdk');

const BASE_DIR = path.resolve(process.env.PHOTOS_BASE_PATH || './storage/photos');
const BUCKET   = process.env.AWS_BUCKET;
const REGION   = process.env.AWS_REGION;

if (!BUCKET || !REGION) {
  console.error('Erro: AWS_BUCKET e AWS_REGION devem estar definidos no .env');
  process.exit(1);
}

const s3 = new AWS.S3({ region: REGION });

async function fetchAllPhotoPaths(client) {
  const queries = [
    `SELECT DISTINCT photo_path AS p FROM clock_records
      WHERE photo_path IS NOT NULL AND photo_path NOT LIKE 'placeholder/%'`,
    `SELECT DISTINCT photo_path AS p FROM clock_photos
      WHERE photo_path IS NOT NULL`,
    `SELECT DISTINCT photo_path AS p FROM service_photos
      WHERE photo_path IS NOT NULL`,
  ];

  const paths = new Set();
  for (const sql of queries) {
    const result = await client.query(sql);
    for (const row of result.rows) paths.add(row.p);
  }
  return [...paths];
}

async function uploadToS3(localPath, key) {
  const buffer = fs.readFileSync(localPath);
  await s3.putObject({
    Bucket:      BUCKET,
    Key:         key,
    Body:        buffer,
    ContentType: 'image/jpeg',
  }).promise();
}

async function main() {
  const client = new Client({ connectionString: process.env.DATABASE_URL });
  await client.connect();

  console.log('Buscando caminhos de fotos no banco...');
  const photoPaths = await fetchAllPhotoPaths(client);
  console.log(`Total de fotos a migrar: ${photoPaths.length}`);

  const errors = [];
  let success  = 0;

  for (let i = 0; i < photoPaths.length; i++) {
    const key       = photoPaths[i];
    const localFile = path.resolve(BASE_DIR, key);
    process.stdout.write(`[${i + 1}/${photoPaths.length}] ${key} ... `);

    if (!fs.existsSync(localFile)) {
      console.log('IGNORADO (arquivo não existe localmente)');
      errors.push({ key, reason: 'arquivo não encontrado localmente' });
      continue;
    }

    try {
      await uploadToS3(localFile, key);
      console.log('OK');
      success++;
    } catch (err) {
      console.log(`ERRO: ${err.message}`);
      errors.push({ key, reason: err.message });
    }
  }

  await client.end();

  console.log(`\nMigração concluída: ${success} OK, ${errors.length} erros.`);
  if (errors.length > 0) {
    console.log('\nArquivos com erro:');
    for (const e of errors) console.log(`  - ${e.key}: ${e.reason}`);
  }
}

main().catch((err) => {
  console.error('Erro fatal:', err);
  process.exit(1);
});
```

- [ ] **Step 3: Commit**

```bash
git add backend/scripts/migrate-to-s3.js
git commit -m "feat(storage): adicionar script de migração de fotos locais para S3"
```

---

## Task 3: Deploy no servidor EC2

**Files:**
- Modify: `/home/ubuntu/pontotools/backend/.env` (no servidor via SSH)

- [ ] **Step 1: Enviar as alterações para o repositório remoto**

```bash
git push origin main
```

- [ ] **Step 2: Conectar ao servidor via SSH**

```bash
ssh -i /c/Users/apoio/Downloads/nicolas.pem ubuntu@56.124.74.200
```

- [ ] **Step 3: Atualizar o código no servidor**

```bash
cd ~/pontotools
git pull origin main
```

Esperado: arquivos `backend/config/storage.js`, `backend/package.json` e `backend/scripts/migrate-to-s3.js` atualizados.

- [ ] **Step 4: Instalar dependências (incluindo aws-sdk)**

```bash
cd ~/pontotools/backend
npm install
```

Esperado: `aws-sdk` instalado em `node_modules/`.

- [ ] **Step 5: Adicionar variáveis S3 ao .env**

```bash
nano ~/pontotools/backend/.env
```

Adicionar ao final do arquivo:
```
STORAGE_DRIVER=s3
AWS_BUCKET=pontotools-fotos
AWS_REGION=sa-east-1
AWS_ACCESS_KEY_ID=<seu Access Key ID>
AWS_SECRET_ACCESS_KEY=<seu Secret Access Key>
```

Salvar e fechar (`Ctrl+O`, `Enter`, `Ctrl+X`).

- [ ] **Step 6: Verificar que as variáveis foram salvas corretamente**

```bash
grep -E "STORAGE_DRIVER|AWS_" ~/pontotools/backend/.env
```

Esperado:
```
STORAGE_DRIVER=s3
AWS_BUCKET=pontotools-fotos
AWS_REGION=sa-east-1
AWS_ACCESS_KEY_ID=...
AWS_SECRET_ACCESS_KEY=...
```

- [ ] **Step 7: Rodar o script de migração**

```bash
cd ~/pontotools/backend
node scripts/migrate-to-s3.js
```

Esperado: log de cada foto com `OK` ou `IGNORADO`. Ao final, `X OK, 0 erros` (idealmente).

- [ ] **Step 8: Reiniciar o backend**

```bash
pm2 restart backend
```

Esperado: processo reiniciado sem erros.

- [ ] **Step 9: Verificar que o backend subiu corretamente**

```bash
pm2 logs backend --lines 20
```

Esperado: logs de inicialização sem erros de conexão ao S3 ou banco.

---

## Task 4: Verificação funcional

- [ ] **Step 1: Testar acesso a uma foto existente**

No frontend em `https://pontotools.shop`, fazer login como admin, abrir o painel de registros de ponto e verificar que uma foto antiga carrega normalmente.

Esperado: foto exibida sem erro 404 ou 500.

- [ ] **Step 2: Registrar um novo ponto de teste**

Fazer login como funcionário e registrar um novo ponto com foto. Verificar no Console AWS (S3 → `pontotools-fotos`) que o arquivo aparece no bucket.

Esperado: objeto novo visível no bucket S3.

- [ ] **Step 3: Verificar acesso à nova foto pelo frontend**

No painel admin, abrir o registro recém-criado e confirmar que a foto aparece normalmente.

Esperado: foto exibida sem erro.

- [ ] **Step 4: Verificar logs do PM2 em busca de erros S3**

```bash
pm2 logs backend --lines 50
```

Esperado: nenhum erro `AccessDenied`, `NoSuchBucket` ou `InvalidAccessKeyId`.
